import { AddButton } from "../components/AddButton";

export default {
  "Default Add Button": (
    <AddButton
      text="추가하기"
      onClick={() => console.log("Add button clicked")}
    />
  ),
};
